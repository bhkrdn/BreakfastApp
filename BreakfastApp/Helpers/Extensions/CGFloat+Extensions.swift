import Foundation

// Extension to define standard spacing constants used throughout the app.
extension CGFloat {
    
    /// Standard small spacing value (e.g., for internal padding within components). Value: 8.0
    static let spacingSmall: CGFloat = 8.0
    
    /// Standard medium spacing value (e.g., padding around text). Value: 12.0
    static let spacingMedium: CGFloat = 12.0
    
    /// Standard large spacing value (e.g., between list items, section padding). Value: 16.0
    static let spacingLarge: CGFloat = 16.0
    
    /// Standard extra-large spacing value (e.g., top/bottom margins for sections). Value: 24.0
    static let spacingExtraLarge: CGFloat = 24.0
    
    // You can add more specific constants if needed, e.g.:
    // static let cornerRadiusStandard: CGFloat = 10.0
} 